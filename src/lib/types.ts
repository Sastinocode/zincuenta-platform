// Tipos generados de la BBDD (Supabase). Pendiente: una vez Sebas haya
// ejecutado las migraciones, generar con
// `npx supabase gen types typescript --project-id <id> > src/lib/types.ts`
export {};
